For small-scale utilities, we wrapped the calls listed in listing \ref{lst:small_scale_wrappers}.

\begin{lstlisting}[label={lst:small_scale_wrappers},caption={Wrapped calls for testing small-scale utilities}]
calloc  creat   execv   fork    malloc  opendir pipe    pthread_create 	realloc
close   execvp  fopen   fstat   mmap    open    poll    pthread_mutex	read    
write
\end{lstlisting}